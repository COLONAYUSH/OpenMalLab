"""The agent roster. Each agent has typed input/output, emits calibrated
confidence, cites facts by id, and can never write a verdict - it proposes, the
Go gate disposes. The analyst agent is the P0 aggregate round-trip; the nine
specialized agents (Router, Correlator, ...) decompose it."""
